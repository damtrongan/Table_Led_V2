// client.list(path, (err, list) => {
//    if (err) {
//      console.error(err);
//      return;
//    }
//    list = list.slice(-1);
//    if (list[0].type === "d") {
//      // If it's a directory, compare its creation timestamp with the current newest folder
//      readDirectory(`${path}/${list[0].name}`);
//    } else {
//      return pathFile = `${path}/${list[0].name}`;
//    }
//  });

 